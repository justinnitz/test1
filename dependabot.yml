version: 2
updates:
  # Configuration for Dockerfile
  

  # Configuration for npm
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    allow:
      - match:
          update_type: "security"
